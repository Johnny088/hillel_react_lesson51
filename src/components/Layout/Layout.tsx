import { SearchForm } from '../SearchForm/SearchForm';
import { UserList } from '../UserList/UserList';

export const Layout = () => {
  return (
    <>
      <SearchForm />
      <UserList />
    </>
  );
};
